venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets -- never commit real API keys
.env

# Downloaded canopy-height tiles (large binaries, safe to re-fetch)
canopy_tile_cache/

# OpenCage geocode result cache and daily-request counter -- local runtime state
opencage_geocode_cache.json
opencage_daily_usage.json

# Ralph-loop local task state -- not project code
.claude/*.local.md
